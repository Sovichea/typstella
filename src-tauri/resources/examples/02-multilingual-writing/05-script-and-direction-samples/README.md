# Script and direction samples

These examples exercise font coverage and Typst rendering across mixed scripts,
bidirectional content, and CJK text.

The bidirectional example demonstrates rendered Typst output only. Typsastra
does not claim first-class RTL editor navigation, selection, deletion, or mixed-
direction conformance in v0.5.1; that work is planned for v0.9.0.
